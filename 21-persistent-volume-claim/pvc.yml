apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: pvc-ns
spec:
  storageClassName: ""
  resources:
    requests:
      storage: 500Mi
  # volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
