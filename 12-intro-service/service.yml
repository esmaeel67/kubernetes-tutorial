apiVersion: v1
kind: Service
metadata:
  name: service-app
  namespace: service-intro
spec:
  selector:
    app: service-app
  ports:
  - port: 80
    # targetPort: 8080
    name: http
    targetPort: http
  - port: 443
    targetPort: http
    name: https
  # sessionAffinity: ClientIP # 
