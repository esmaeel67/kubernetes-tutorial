apiVersion: v1
kind: Service
metadata:
  name: service-app
  namespace: service-intro
spec:
  selector:
    app: service-app
  ports:
  - port: 80
    targetPort: 8080
    # targetPort: http
    # name: http
  # - port: 443
  #   targetPort: 8443
  #   name: https
  sessionAffinity: ClientIP # 
