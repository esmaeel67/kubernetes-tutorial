apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: app
  namespace: service-intro
  labels:
    name: app
spec:
  replicas: 3
  selector:
    matchLabels:
      name: app
  template:
    metadata:
      name: app
      labels:
        name: app
    spec:
      containers:
      - image: esmaeil157/demo-app:v1.0.0
        name: app
        ports:
        - containerPort: 8080
          protocol: TCP
          name: http
        livenessProbe:
          httpGet:
            port: http
            path: /